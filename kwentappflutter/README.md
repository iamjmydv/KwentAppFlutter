# Kwentapp — Flutter app

The Flutter project. **Full documentation lives in the [repository README](../README.md)** — features, architecture, data model, the RLS policy matrix, and the decisions behind them.

```bash
flutter pub get
flutter run -d chrome
```
